<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=
    , initial-scale=1.0"
    />
    <title>dashboard</title>
  </head>
  <body>
    <%if(emailname){%>
    <p>Welcome <%= emailname %></p>
    <form method="post" action="/dashboard">
      <label>Create A new Message</label>
      <input name="message" id="message" type="text" required />
      <input type="submit" value="submit" />
    </form>
    <%}else{%>
    <p>you cannot create a message unless you're not logged in</p>
    <%}%>
  </body>
</html>
